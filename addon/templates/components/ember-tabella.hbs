{{#if hasHeader}}
  <div class="ember-tabella__header">
    {{ember-tabella/header-row 
      columns=_columns 
      height=headerHeight 
      width=headerRowWith
      scrollLeft=scrollLeft 
      sortProperties=sortProperties
      isSortReversed=isSortReversed
      on-sort=(action "onSort")
    }}
  </div>
{{/if}}

{{ember-tabella/body 
  rows=content 
  columns=_columns 
  height=bodyHeight 
  width=rowWidth 
  rowWidth=rowWidth
  rowHeight=rowHeight
  scrollLeft=scrollLeft
  scrollTop=scrollTop
  scroll-change=(action "scrollChange")}}
